#include "main.h"

/**
 * _strlen - functiun that returns the length of a string
 * @s: pointer to the string of characters
 *
 * Return: Length of the string
 */
int _strlen(char *s)
{
	int length;

	for (length = 0; s[length] != '\0'; length++)
	{
	}

	return (length);
}

