#include "main;h"

/**
 * _add - function that adds two integers and return the result
 * @n: value of the add
 *
 * Return: Always 0 (Success)
 */
int add(int a, int b);
{
	int n = a + b;

	_printf("%d\n", n);

	return (0);
}
