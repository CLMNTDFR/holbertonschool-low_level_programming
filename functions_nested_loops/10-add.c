#include "main.h"

/**
 * add - function that adds two integers and return the result
 * @a: value of first
 * @b: value of second
 *
 * Return: Always 0 (Success)
 */
int add(int a, int b)
{
	return (a + b);
}
