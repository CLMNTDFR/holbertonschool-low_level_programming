#include "main.h"

/**
 * _add - function that adds two integers and return the result
 * @n: value of the add
 * @a: value of first
 * @b: value of second
 *
 * Return: Always 0 (Success)
 */
int add(int a, int b);
{
	int n = a + b;

	_printf("%d\n", n);

	return (0);
}
