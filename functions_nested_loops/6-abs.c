#include "main.h"

/**
 * _abs -  function that computes the absolute value of an integer
 * @r: the number to be compute
 *
 * Return: Absolute value of a number
 */
int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	else if (r == 0)
	{
		return (r);
	}
	else
	{
		return (r);
	}
}

